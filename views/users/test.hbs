<section>
    <div class="container d-flex justify-content-center align-items-center">
  {{#each products}}
    <div class="card me-2">
      <span class="percent">{{this.Discount}}</span>
      <div class="card-image">
        <img src="product_images/{{this._id}}.jpg" width="250" />
      </div>
      <div class="card-inner">
        <span>{{this.Category}}</span>
        <h5 class="mb-0">{{this.Name}}</h5>
        <div class="price">
          <span>₹{{this.Price}}</span>
          <!--  offer price <sup>$20.99</sup> -->
        </div>
        <div class="mt-3 d-flex justify-content-between align-items-center">
          <a href="/view_details"
            class="btn btn-success text-uppercase btn-sm details"
          >Details</a>
          <div class="d-flex flex-row">
           <!--  whishilist button
           <span class="wishlist"><i class="fa fa-heart"></i></span> -->
           <button class="cart" onclick="add_to_cart('{{this._id}}')"><i class="fa fa-shopping-cart"></i></button>
          </div>
        </div>
      </div>
    </div>
  {{/each}}
</div>
</section>

<script>
    function add_to_cart(product_id){
    $.ajax({
        url:'/add_to_cart?id='+product_id,
        method:'get',
        success:(response)=>{
            console.log('test 38',response)
            if(response.stat){
                let count=$('#cart_count').html()
                count=parseInt(count)+1
                $("#cart_count").html(count)
            }
            else{
              location.href = "/login"

            }
        }

    })  
}
</script>